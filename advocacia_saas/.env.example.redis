# Redis Configuration for Render
# Add these environment variables in your Render dashboard

# Redis URL (provided by Render when you create Redis instance)
REDIS_URL=redis://username:password@host:port

# Optional: Separate databases for different purposes
REDIS_CACHE_DB=0
REDIS_RATELIMIT_DB=1
REDIS_SESSION_DB=2

# Cache settings
CACHE_DEFAULT_TIMEOUT=300
CACHE_KEY_PREFIX=petitio

# Rate limiting
RATELIMIT_ENABLED=true